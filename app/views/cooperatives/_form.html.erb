
<%= simple_form_for(@cooperative) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :short_description %>
    <%= f.association :segment %>
    <%= f.input :minimum_of_associates %>
    <%= f.input :value_minimum_to_start, input_html: { type: 'text', class: 'money' } %>
    <%= f.input :indicators %>
    <%= f.input :development_time_in_days %>
    <%= f.input :share_capital, input_html: { type: 'text', class: 'money' } %>
    <%= f.input :video_url %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
